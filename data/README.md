# Data

This release includes the `RawData/gpkg/` geospatial layers, surrogate tabular assets in `surrogate/data/`, released optimization result JSON files in `CoOpt/results/`, and input schema examples for real-block matching.

The full `RealDis/dataset.csv` matching input is user-provided and should follow `RealDis/dataset.csv.example`.
